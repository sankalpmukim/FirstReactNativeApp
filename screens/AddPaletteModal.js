import React from 'react';
import { View, Text } from 'react-native';

const AddNewPalette = () => {
  return (
    <View>
      <Text>Add new palette modal</Text>
    </View>
  );
};

export default AddNewPalette;
